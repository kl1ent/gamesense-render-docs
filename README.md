---

# 🧾 Документация: `render`, `vec2_t`, `col_t`

---

## 🧱 Модуль: `render`

### 📘 Описание

`render` — основной API для рисования 2D-графики через ImGui-рендерер (`imgui_render.dll`).
Позволяет рисовать фигуры, текст, изображения, применять эффекты (тени, размытие) и управлять рендер-циклом.

---

### ⚙️ Функции

#### 🔤 `render.load_font(path: string, size?: number, flags?: number) -> string`

Загружает и регистрирует шрифт.

| Параметр | Тип               | Описание                              |
| -------- | ----------------- | ------------------------------------- |
| `path`   | `string`          | Путь к файлу шрифта                   |
| `size`   | `number` *(опц.)* | Размер шрифта (по умолчанию `12`)     |
| `flags`  | `number` *(опц.)* | Флаги ImGui-шрифта (по умолчанию `0`) |

📤 **Возвращает:** строковый идентификатор шрифта (используется в `render.add_text`).

---

#### 🖼️ `render.load_image(content: string) -> ffi.cdata*`

Загружает изображение из файла или бинарных данных.

| Параметр  | Тип      | Описание                                       |
| --------- | -------- | ---------------------------------------------- |
| `content` | `string` | Путь к изображению или его бинарное содержимое |

📤 **Возвращает:** созданную текстуру.

---

#### 🅰️ `render.get_text_size(font: string, text: string) -> vec2_t`

Возвращает размеры текста в пикселях.

| Параметр | Тип      | Описание                                   |
| -------- | -------- | ------------------------------------------ |
| `font`   | `string` | ID шрифта, возвращённый `render.load_font` |
| `text`   | `string` | Текст                                      |

📤 **Возвращает:** `vec2_t(width, height)`.

---

#### 🖋️ `render.add_text(font: string, text: string, x: number, y: number, color: col_t)`

Рисует текст.

| Параметр | Тип      | Описание     |
| -------- | -------- | ------------ |
| `font`   | `string` | ID шрифта    |
| `text`   | `string` | Текст        |
| `x`      | `number` | Координата X |
| `y`      | `number` | Координата Y |
| `color`  | `col_t`  | Цвет текста  |

---

#### 🟦 `render.add_rect_filled(x: number, y: number, w: number, h: number, color: col_t, rounding?: number, flags?: number)`

Рисует **залитый прямоугольник**.

| Параметр   | Тип               | Описание                          |
| ---------- | ----------------- | --------------------------------- |
| `x`        | `number`          | Координата X верхнего левого угла |
| `y`        | `number`          | Координата Y верхнего левого угла |
| `w`        | `number`          | Координата X нижнего правого угла |
| `h`        | `number`          | Координата Y нижнего правого угла |
| `color`    | `col_t`           | Цвет                              |
| `rounding` | `number` *(опц.)* | Радиус скругления углов           |
| `flags`    | `number` *(опц.)* | Флаги ImGui                       |

---

#### ⬛ `render.add_rect(x: number, y: number, w: number, h: number, color: col_t, thick?: number, rounding?: number, flags?: number)`

Рисует **рамку прямоугольника**.

| Параметр   | Тип               | Описание                            |
| ---------- | ----------------- | ----------------------------------- |
| `x`        | `number`          | Координата X верхнего левого угла   |
| `y`        | `number`          | Координата Y верхнего левого угла   |
| `w`        | `number`          | Координата X нижнего правого угла   |
| `h`        | `number`          | Координата Y нижнего правого угла   |
| `color`    | `col_t`           | Цвет рамки                          |
| `thick`    | `number` *(опц.)* | Толщина линии (по умолчанию `1`)    |
| `rounding` | `number` *(опц.)* | Скругление углов (по умолчанию `0`) |
| `flags`    | `number` *(опц.)* | Флаги                               |

---

#### 🌫️ `render.add_blur(x: number, y: number, w: number, h: number, color: col_t, radius?: number, rounding?: number, flags?: number)`

Добавляет эффект размытия области.

| Параметр           | Тип               | Описание                   |
| ------------------ | ----------------- | -------------------------- |
| `x`, `y`, `w`, `h` | `number`          | Координаты области         |
| `color`            | `col_t`           | Цвет/интенсивность эффекта |
| `radius`           | `number` *(опц.)* | Радиус размытия            |
| `rounding`         | `number` *(опц.)* | Скругление краёв           |
| `flags`            | `number` *(опц.)* | Флаги                      |

---

#### 🧊 `render.add_rect_shadow(x: number, y: number, w: number, h: number, color: col_t, thick?: number, offset?: vec2_t, rounding?: number, flags?: number)`

Рисует **тень под прямоугольником**.

| Параметр           | Тип               | Описание                  |
| ------------------ | ----------------- | ------------------------- |
| `x`, `y`, `w`, `h` | `number`          | Координаты прямоугольника |
| `color`            | `col_t`           | Цвет тени                 |
| `thick`            | `number` *(опц.)* | Радиус размытия           |
| `offset`           | `vec2_t` *(опц.)* | Смещение тени             |
| `rounding`         | `number` *(опц.)* | Скругление углов          |
| `flags`            | `number` *(опц.)* | Флаги                     |

---

#### 🧭 `render.add_line(x: number, y: number, x1: number, y1: number, color: col_t, thick?: number)`

Рисует **линию** между двумя точками.

---

#### 🌀 `render.add_circle(x: number, y: number, radius: number, segments: number, color: col_t, thick?: number)`

Рисует **контур круга**.

---

#### ⚫ `render.add_circle_filled(x: number, y: number, radius: number, segments: number, color: col_t)`

Рисует **залитый круг**.

---

#### 🧮 `render.add_polyline(points: table<vec2_t>, color: col_t, thick?: number, flags?: number)`

Рисует **многоугольную линию (polyline)**.

| Параметр | Тип               | Описание     |
| -------- | ----------------- | ------------ |
| `points` | `table<vec2_t>`   | Массив точек |
| `color`  | `col_t`           | Цвет         |
| `thick`  | `number` *(опц.)* | Толщина      |
| `flags`  | `number` *(опц.)* | Флаги        |

---

#### 🔺 `render.add_polyfilled(points: table<vec2_t>, color: col_t)`

Рисует **залитый многоугольник**.

---

#### 🌑 `render.add_polyshadow(points: table<vec2_t>, color: col_t, thick?: number, offset?: vec2_t, flags?: number)`

Добавляет **тень для полигона**.

---

#### ✂️ `render.push_clip_rect(x: number, y: number, w: number, h: number, intersect?: boolean)`

Устанавливает **область обрезки** (clip rect).

---

#### ✂️ `render.pop_clip_rect()`

Сбрасывает текущую область обрезки.

---

#### 🔁 `render.set_callback(fn: function)`

Добавляет функцию, вызываемую каждый кадр.

Пример:

```lua
render.set_callback(function()
    render.add_rect_filled(50, 50, 200, 100, render.col_t(0, 0.5, 1, 1), 5)
end)
```

---

## 📐 Модуль: `vec2_t`

### 📘 Описание

`vec2_t` — структура двухмерного вектора, используется для координат, размеров и смещений.

---

### 🧱 Конструктор

```lua
local v = render.vec2_t(x, y)
```

| Параметр | Тип      | Описание     |
| -------- | -------- | ------------ |
| `x`      | `number` | Координата X |
| `y`      | `number` | Координата Y |

---

### ⚙️ Поддерживаемые операции

| Оператор | Описание           | Пример         |
| -------- | ------------------ | -------------- |
| `+`      | Сложение векторов  | `v3 = v1 + v2` |
| `-`      | Вычитание          | `v3 = v1 - v2` |
| `*`      | Умножение на число | `v2 = v1 * 2`  |

---

### 🧩 Методы

#### `v:unpack() -> number, number`

Возвращает `x, y`.

#### `tostring(v) -> string`

Возвращает строку:

```lua
vec2_t(10.00, 5.00)
```

---

## 🎨 Модуль: `col_t`

### 📘 Описание

`col_t` — структура цвета RGBA.
Используется во всех функциях рендеринга.

---

### 🧱 Конструктор

```lua
local c = render.col_t(r, g, b, a)
```

| Параметр | Тип               | Диапазон | Описание                       |
| -------- | ----------------- | -------- | ------------------------------ |
| `r`      | `number`          | 0.0–1.0  | Красный                        |
| `g`      | `number`          | 0.0–1.0  | Зелёный                        |
| `b`      | `number`          | 0.0–1.0  | Синий                          |
| `a`      | `number` *(опц.)* | 0.0–1.0  | Альфа-канал (по умолчанию `1`) |

---

### 🧩 Методы

#### `c:unpack() -> number, number, number, number`

Возвращает `r, g, b, a`.

#### `tostring(c) -> string`

Возвращает строку:

```lua
col_t(1.00, 0.50, 0.00, 1.00)
```

---

## 💡 Пример использования

```lua
local render = require( 'render' )

local col_t = render.col_t
local vec2_t = render.vec2_t

local bytes = ""
local image_bytes = render.load_image( bytes )
local image_file = render.load_image( 'D:/Steam/steamapps/common/Counter-Strike Global Offensive/img.jpg' )

local font = render.load_font( 'C:/Windows/Fonts/Verdana.ttf', 40 )

render.set_callback( function( )
    local size = render.get_text_size( font, 'morion.pw' )

    render.add_image( image_bytes, 100, 100, 20, 20, col_t( 1, 1, 1, 1 ) )
    render.push_clip_rect( 400, 100, 200, 200 )
    render.add_image( image_file, 400, 100, 736 / 2, 693 / 2, col_t( 1, 1, 1, 1 ), 30 )
    render.pop_clip_rect( )

    render.add_blur( 477, 180, size.x + 10, size.y + 10, col_t( 1, 1, 1, 1 ), 4, 10 )
    render.add_rect_shadow( 100, 100, 736 / 2, 693 / 2, col_t( 0, 0, 0, 1 ) )
    render.add_rect_filled( 175, 185, size.x + 10, size.y + 10, col_t( 1, 1, 1, 1 ), 10 )
    render.add_rect( 477, 180, size.x + 10, size.y + 10, col_t( 1, 1, 1, 1 ), 3, 10 )
    render.add_text( font, 'morion.pw', 482, 181, col_t( 1, 1, 1, 1 ) )

    local polygons = { vec2_t( 125, 100 ), vec2_t( 162, 150 ), vec2_t( 200, 100 ) }
    render.add_line( polygons[1].x, polygons[1].y, polygons[2].x, polygons[2].y, col_t( 1, 1, 1, 1 ) )

    render.add_polyshadow( polygons, col_t( 0, 0, 0, 1 ) )
    render.add_polyfilled( polygons, col_t( 1, 0, 0, 1 ) )
    render.add_polyline( polygons, col_t( 1, 1, 1, 1 ), 3, bit.lshift( 1, 0 ) )

    render.add_circle_filled( 200, 200, 30, 25, col_t( 1, 0, 0, 1 ) )
    render.add_circle( 200, 200, 30, 25, col_t( 1, 1, 1, 1 ), 3 )
    render.add_circle_shadow( 200, 200, 30, 25, col_t( 1, 1, 1, 1 ) )
end )
```
